$.index.open();

Alloy.Collections.house.fetch();
Alloy.Collections.estate.fetch();

function showId(e) {
	console.log(e.value);
}

function getHouseInfo(model) {
	var transform = model.toJSON();
	
	return transform;
}
